import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />); // lo que sea que este dentro del archivo App
// los vamos a renderizar y lo vamos a insertar en nuestro index.html
// con el id de root
